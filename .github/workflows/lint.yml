name: Lint

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder lint step
        run: |
          echo "Lint checks placeholder - passing"
          exit 0 